alpine:
  image: "alpine:3.2"
  command: ["/bin/sh"]
default:
  image: "docker-registry.ai-traders.com/gitide:0.2.0"
  links:
  - alpine
  volumes:
  - ${IDE_IDENTITY}:/ide/identity:ro
  - ${IDE_WORK}:/ide/work
  env_file:
  - ${ENV_FILE}
