version: '2'
services:
  default:
    image: alpine:3.4
    volumes:
      - ${IDE_IDENTITY}:/ide/identity:ro
      - ${IDE_WORK}:/ide/work
    env_file:
      - ${ENV_FILE}
  another-alpine:
    image: alpine:3.4
    env_file:
      - ${ENV_FILE}
